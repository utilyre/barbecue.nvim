[changelog]
header = ""
body = """
All changes of this release are documented here.

**NOTE**: Police revolving light emoji (🚨) indicates that breaking changes have
happened on the commit.
{% for group, commits in commits | group_by(attribute="group") %}
  ## {{ group }}
  {% for commit in commits %}
    - `{{ commit.id | truncate(length=7,end="") }}`: {% if commit.breaking %}🚨 {% endif %}{{ commit.message | upper_first }}.
  {% endfor %}
{% endfor %}
"""
footer = """
<!-- generated by [git-cliff](https://github.com/orhun/git-cliff) -->
"""

[git]
commit_parsers = [
  { message = "^feat", group = "Features" },
  { message = "^fix", group = "Bug Fixes" },
  { message = "^doc", group = "Documentation" },
  { message = "^perf", group = "Performance" },
  { message = "^refactor", group = "Refactor" },
  { message = "^style", group = "Styling" },
  { message = "^test", group = "Testing" },
  { message = "^chore", group = "Miscellaneous Tasks"},
]
